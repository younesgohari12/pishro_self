# گزارش بررسی و اصلاح — نسخه 0.09.2

نتیجه: در نسخه 0.09.1 چند ایراد دیگر وجود داشت که اصلاح شدند. **۱۲۸ تست پاس، صفر شکست**؛ شامل ۳۱ تست تازه نسبت به نسخه قبلی. این نتیجه مربوط به سناریوهای تست‌شده است و تضمین نبودن همه باگ‌های ممکن نیست.

## یافته‌ها و اصلاحات

| بخش | ایراد | اصلاح |
|---|---|---|
| دسترسی مدیر | کاربر دیگری با نام کاربری قبلی مدیر می‌توانست در مسیر fallback مجوز بگیرد؛ رکورد قدیمی بدون ID نیز خودکار قابل تصاحب بود. | مجوز صرفاً با ID عددی کنترل می‌شود. نام کاربری فقط داده نمایشی است و نقش مدیر دیگری را جابه‌جا نمی‌کند. |
| تنظیمات مدیر | فرم باز تنظیمات پس از سلب مجوز system_settings همچنان قابل استفاده بود. | مجوز هنگام دریافت پاسخ فرم نیز دوباره بررسی می‌شود. |
| پرداخت | شارژ موجودی و تأیید درخواست در دو محل جدا انجام می‌شد؛ خطای میانه ثبت می‌توانست امکان شارژ دوباره را باقی بگذارد. | درخواست و تصمیم پرداخت در SQLite نگهداری می‌شوند؛ شارژ، دفتر تراکنش و تغییر وضعیت اتمی و تکرارناپذیرند. ایجاد درخواست هم‌زمان نیز فقط یک pending برای کاربر ایجاد می‌کند. |
| دکمه‌های خرید | دکمه خرید قدیمی می‌توانست مبلغ یا مرحله تأیید انتقال را عوض کند. | هر دکمه فقط در مرحله مجاز خرید پذیرفته می‌شود. |
| تداخل گفتگوها | شروع ورود، وضعیت قبلی تیکت/فرم‌های دیگر را پاک نمی‌کرد؛ ورودی ورود ممکن بود در مسیر اشتباه پردازش شود. | شروع ورود فرم‌های قبلی را پاک می‌کند؛ تا پایان/لغو ورود، ورود به فرم‌های دیگر متوقف است. /start و بازگشت به منوی اصلی وضعیت‌ها را پاک می‌کنند. |
| چرخه ورود | لغو یا خطای درخواست قدیمی می‌توانست نشست تازه را پاک کند؛ شکست ارسال پیام خطا می‌توانست اتصال را باز بگذارد. | پاک‌سازی به همان تلاش ورود متصل است و قبل از اعلان خطا انجام می‌شود. نشست رهاشده با زمان‌سنج پاک و قطع می‌شود. درخواست‌های شبکه ورود timeout دارند. |
| ذخیره و تکمیل ورود | فایل سشن مستقیماً بازنویسی می‌شد؛ ناتوانی در شروع سرویس با پیام موفقیت کامل نمایش داده می‌شد. | ذخیره سشن با فایل موقت خصوصی و جایگزینی اتمی انجام می‌شود. وضعیت «اتصال ذخیره شد» از «اجرای سرویس» جداست. عضویت اجباری پیش از ذخیره نیز بررسی می‌شود. |
| اعتبار و صورتحساب | تمام‌شدن موجودی لزوماً اجرای سلف را متوقف نمی‌کرد؛ زمان غیرمتناهی می‌توانست حلقه محاسبه مصرف را گیر بیندازد؛ task صورتحساب پس از توقف ربات پاک نمی‌شد. | اجرای اصلی به اعتبار یا Trial فعال وابسته است. مصرف و زمان باقی‌مانده در یک تراکنش ثبت می‌شوند؛ فقط مقدار واقعاً کسرشده محاسبه می‌شود. زمان نامعتبر مدیریت و taskهای پس‌زمینه هنگام خروج لغو می‌شوند. |
| زمان‌بند تبچی | وضعیت اعتبار/خاموشی در زمان ارسال بررسی نمی‌شد و FloodWait بیشتر از یک ساعت به یک ساعت کوتاه می‌شد. | وضعیت دسترسی پیش از ارسال کنترل می‌شود؛ زمان کامل اعلام‌شده تلگرام رعایت می‌شود، از جمله ۴۰۷۹۳ ثانیه. |

## تست و شواهد

- ۱۲۸ تست، صفر شکست؛ دستور: `python -m pytest -q tests --disable-warnings`.
- تست‌های پیش از اصلاح، خطاهای مجوز، دکمه قدیمی و پاک‌سازی ورود را بازتولید کردند؛ تست تداخل فرم پشتیبانی/ورود نیز ابتدا شکست خورد و پس از اصلاح پاس شد.
- تست پرداخت با ۲۰ تأیید هم‌زمان: فقط یک شارژ.
- خطای عمدی هنگام تغییر وضعیت پرداخت: هم موجودی و هم دفتر تراکنش rollback شدند؛ تکرار بعدی فقط یک‌بار شارژ کرد.
- خطای نوشتن شمارنده‌های نمایشی پس از تأیید: درخواست همچنان approved ماند و دوباره شارژ نشد.
- تست‌های مصرف، انقضای Trial، حساب بدون اعتبار، خرابی ذخیره سشن، انقضای نشست و کنترل مجدد عضویت اجرا شدند.
- تست زمان‌بند برای احترام به انتظار ۴۰۷۹۳ثانیه‌ای و جلوگیری از ارسال حساب فاقد دسترسی پاس شد.
- مهاجرت روی کپی دیتابیس داخل فایل ارسالی، و اجرای دوباره همان مهاجرت، پاس شد؛ تعداد رکوردهای تمام جدول‌های قبلی حفظ شد. داده‌ها و سشن‌های فایل اصلی تغییر نکردند.
- تمام فایل‌های Python از نظر syntax بررسی شدند. تست‌ها از دیتابیس موقت استفاده کردند و اتصال شبکه در آنها مسدود بود.

## نصب روی نسخه موجود

1. فرایندهای ربات را متوقف کنید و از کل پروژه، به‌ویژه `db` و `sessions`، پشتیبان بگیرید.
2. محتوای پوشه `0.09` بسته را روی پروژه فعلی کپی کنید. اگر `config.py` سرور را بعد از فایل ارسالی تغییر داده‌اید، تنظیمات فعلی سرور را حفظ کنید.
3. پوشه‌های زمان اجرا `db`، `sessions`، `message_cache` و رسانه‌های کاربر در بسته نیستند؛ **آنها را حذف نکنید**.
4. با همان روش قبلی `main.py` را اجرا کنید. جدول‌های تازه بدون حذف جدول‌های قبلی ایجاد می‌شوند.
5. پرداخت‌های JSON قبلی یک‌بار به `wallet_payments` وارد می‌شوند. از این نسخه به بعد، مرجع پرداخت همین جدول SQLite است؛ تغییر دستی `payments.json` وضعیت پرداخت را تغییر نمی‌دهد.
6. ادمین‌های دارای ID عددی حفظ می‌شوند. اگر ادمین قدیمی فقط با Username ذخیره شده، مالک باید او را دوباره با ID عددی اضافه کند؛ این رکوردها خودکار صاحب جدید پیدا نمی‌کنند.
7. حساب دارای موجودی صفر و بدون Trial فعال، اتصالش قابل ذخیره است اما سرویس پولی اجرا نمی‌شود. برای استفاده، کیف پول را شارژ یا Trial معتبر را فعال کنید.

## حدود بررسی و موارد باقی‌مانده

- ارسال کد، تحویل پیام، مجوزهای تلگرام و اجرای واقعی سشن روی حساب شما در این محیط تست نشده‌اند. سرور و سشن واقعی متصل نشدند.
- محدودیت FloodWait تلگرام قابل حذف از کد ربات نیست؛ نسخه جدید آن را رعایت می‌کند. محدودیت درخواست محلی ورود در حافظه فرایند نگهداری می‌شود؛ پس از ری‌استارت، محدودیت سروری همچنان برقرار است.
- شمارنده‌های آماری قدیمی JSON هنوز پس از ثبت مالی به‌روزرسانی می‌شوند. در خطای نوشتن فایل ممکن است شمارنده نمایشی عقب بماند؛ موجودی و وضعیت مالی SQLite معتبرند و عملیات دوباره انجام نمی‌شود. این بخش هنوز نیازمند یکسان‌سازی کامل گزارش‌گیری با دفتر مالی است.
- این نسخه برای همان اجرای تک‌نمونه‌ای قبلی تهیه شده است؛ اجرای چند نمونه مستقل از کل ربات و زمان‌بند روی یک مجموعه JSON بررسی نشده است.
- بازپرداخت انتقال‌های جعلی تاریخی انجام نشده؛ اصلاح موجودی تاریخی به بررسی موردی رسیدها نیاز دارد.

فایل `SECURITY_FIX_v0.09.1_FA.md` گزارش تاریخی نسخه قبلی است. برای نصب و وضعیت فعلی، همین فایل را مبنا قرار دهید.
