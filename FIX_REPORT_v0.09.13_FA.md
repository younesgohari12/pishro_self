# گزارش نسخه 0.09.13

- ترجمه: ورودی خام متن جایگزین JSON شد؛ خروجی‌های پوشش‌دار تصادفی به متن ساده تبدیل می‌شوند. JSON و کد عمدی متن ورودی حفظ می‌شوند. کش v3 پاسخ نامناسب نسخه قبل را تکرار نمی‌کند. پیام نهایی فقط متن ترجمه است.
- قیمت ارز: دستور `.ارز` و گزینه پنل سلف فعال‌اند. ربات اصلی همچنان فلگ مستقل قبلی خود را دارد. نام فارسی رایج، نام انگلیسی، نماد و شناسه کامل CoinGecko پشتیبانی می‌شوند. نام فارسی ناشناخته با کمک AvalAI شناسایی و با فهرست واقعی CoinGecko تأیید می‌شود؛ AI هیچ قیمتی تولید نمی‌کند. ابهام نماد با پیشنهاد شناسه‌ها حل می‌شود.
- کلید ارسالی CoinGecko در کانفیگ قرار گرفت. اولین اجرای main پس از بکاپ، فقط تنظیمات مجاز CoinGecko را در کانفیگ پایدار به‌روز می‌کند. ثبت کلید و نشانگر به‌روزرسانی اتمیک است؛ تغییر دستی بعدی کاربر حفظ می‌شود.
- قیمت دلار از CoinGecko دریافت می‌شود. تومان با نرخ تتر/ریال نوبیتکس و تقسیم بر ۱۰ محاسبه می‌شود؛ در خطا، بازار تتر/تومان والکس جایگزین است. نام منبع و زمان دریافت نمایش داده می‌شود. در قطع هر دو منبع، تومان ناموجود است و دلار معتبر باقی می‌ماند.
- قیمت و زمان نامعتبر/کهنه، مقادیر NaN/Infinity و قیمت صفر رد می‌شوند. قیمت بسیار کوچک به صفر گرد نمی‌شود. درخواست‌های همزمان یک ارز مشترک‌اند؛ کش کوتاه، محدودیت کاربر و توقف پس از HTTP 429 برقرار است.
- کلید CoinGecko فقط به CoinGecko ارسال می‌شود و دنبال‌کردن redirect برای درخواست‌های قیمت غیرفعال است. اتصال HTTP تنظیم پراکسی استاندارد سیستم را رعایت می‌کند؛ فایل env برای تنظیمات برنامه اضافه نشده است.
- حسابگر نیم‌ساعتی، سرویس موجودی، جدول‌های دیتابیس، کد مهاجرت کیف پول و بازیابی سشن در مقایسه با نسخه 0.09.12 تغییر نکرده‌اند. ساختار داده فرمت ۲ حفظ شده است.

اعتبارسنجی: ۴۳۹ تست موفق و بررسی syntax برای ۱۰۵ فایل Python. تست واقعی قیمت بیت‌کوین با کد پروژه، دلار و تومان مثبت دریافت کرد؛ نرخ تومان از والکس بود، چون نوبیتکس از این محیط پاسخ نداد. فهرست واقعی ۲۱٬۱۷۲ ارز خوانده شد. هیچ اتصال به حساب تلگرام یا درخواست پولی به AvalAI/Google برای آزمایش انجام نشد. رفع خروجی ترجمه با پاسخ‌های شبیه‌سازی‌شده و هندلر واقعی بررسی شد.

راهنمای استفاده و ارتقا در README_FIRST_FA.md است. برای حفظ داده، نسخه جدید را در پوشه جداگانه و با همان کاربر سیستم‌عامل اجرا کنید.
