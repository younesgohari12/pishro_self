# گزارش اصلاح Premium Resend — حذف کامل Edit + دستورات فارسی

**نسخه:** v0.09.13 (ریلیز `premium-resend-fa`)
**تاریخ:** ۱۷ سپتامبر ۲۰۲۶ / ۲۶ شهریور ۱۴۰۵
**وضعیت:** ✅ تکمیل و تست‌شده — ۸۳۴ تست پاس (بدون رگرسیون)

---

## ۱) مشکل حل‌شده: Edit ممنوع → Delete + New Send واقعی

### رفتار قبلی (باگ)
پیام اصلی **Edit می‌شد** (EditMessageRequest)؛ در نتیجه Custom Emoji در
خیلی از چت‌ها روی پیام نمی‌ماند و قابلیت عملاً کار نمی‌کرد.

### رفتار جدید (دقیقاً مطابق spec مالک)

```
Original Message
        |
  Copy Content          (متن، Entity، Reply، عکس/ویدیو/فایل، کپشن، silent)
        |
Delete Original Message
        |
Send New Message With Custom Emoji Entity
```

1. پیام معمولی ارسال می‌شود.
2. بررسی می‌شود که **Telegram Custom Emoji Entity واقعی** داخل پیام هست یا
   نه — تصمیم بر اساس **نمای واقعی سرور** است (`client.get_messages` بعد از
   مکث ۰٫۵ ثانیه)، نه شیء محلی Telethon.
3. اگر Entity وجود نداشت و نگاشت ایموجی موجود بود:
   - ❌ **EditMessage مطلقاً ممنوع** — نه `edit_message`، نه
     `EditMessageRequest`، نه تغییر همان پیام.
   - ✅ پیام اصلی **DELETE** و یک **پیام جدید جداگانه** با Entity ارسال می‌شود.

**نتیجه در چت:** پیام قدیمی حذف شده و فقط یک پیام جدید (با Custom Emoji)
باقی می‌ماند.

### پوشش کامل قوانین Resend
برای همه موارد زیر جریان **Delete + New Send** است و **هیچ Edit مجاز نیست**:
متن، Reply، عکس، ویدیو، فایل، Caption و آلبوم (کل آلبوم یک‌جا؛ نه قطعه‌قطعه).
همه چت‌ها: Saved، Private، Group و Channel (با اجازه).

---

## ۲) ماژول جدید: `services/emoji_resend_manager.py`

- **مسئولیت‌ها:** بررسی Entity (نمای سرور)، کپی محتوا، حذف پیام، ارسال دوباره.
- **هرگونه منطق Edit از این سیستم حذف شده است.** راستی‌آزمایی AST در تست‌ها:
  هیچ فراخوانی `edit_message` / `EditMessageRequest` در کل ماژول وجود ندارد.
- `services/premium_resend.py` اکنون فقط یک shim سازگاری است که نمادهای
  قدیمی (`install_premium_resend` و ...) را از ماژول جدید بازنشر می‌کند.

### قواعد امنیتی حفظ‌شده
- ابتدا محتوا کامل در حافظه کپی می‌شود؛ سپس حذف و بلافاصله ارسال.
- اگر **حذف شکست بخورد**، نسخه جدید هرگز ارسال نمی‌شود (هیچ‌گاه دو نسخه از
  یک پیام در چت باقی نمی‌ماند).
- اگر **ارسال شکست بخورد**: ۳ تلاش مجدد کوتاه + یک تلاش نجات‌دهندهٔ نهایی
  بدون Entity برای اینکه محتوا هرگز گم نشود؛ نتیجه در بلوک گزارش می‌شود.
- ضد race: قفل per-chat + حافظه پیام‌های خودمان (loop guard) + cooldown
  موتور کانورتر (اگر سرور Entity را مکرراً حذف کند) + یک تلاش برای هر پیام.

### تغییرات فنی دیگر
- `install_premium_emoji_outgoing_injector` (در converter) دیگر **فقط**
  پیام را به مدیر ارسال دوباره می‌سپارد؛ مسیر edit-fallback کامل حذف شد.
- `edit_message` دیگر توسط کانورتر wrap نمی‌شود (Pipeline فقط
  send_message / send_file / _send_album). edit های سایر قابلیت‌ها (مثل
  بنر تبچی) بدون دخالت ایموجی ویژه عبور می‌کنند.
- بلوک‌های لاگ: `[PREMIUM_CHECK]` و `[PremiumResend]` → فارسی شدند (بخش ۴).

---

## ۳) نام فارسی قابلیت‌ها (پنل و دستورات)

| نام قبلی | نام جدید |
|---|---|
| Premium Emoji | **ایموجی ویژه** |
| Premium Resend / Resend هوشمند | **ارسال دوباره ایموجی ویژه** |
| Premium Debug | **بررسی ایموجی ویژه** |
| Premium Status | **وضعیت ایموجی ویژه** |
| Away Message | **پیام عدم حضور** |
| Close (خروج) | **بستن عملیات** |

تمام دکمه‌های پنل، پیام‌های تأیید toggle، منوی ✨ ایموجی ویژه و گزارش‌های
کانال ادمین («🎨 ایموجی ویژه تبدیل شد»، «⚠️ ایموجی ویژه — جایگزین»،
«❌ خطای ایموجی ویژه») فارسی شدند.

---

## ۴) دستورات فارسی جدید

| دستور | عملکرد |
|---|---|
| `.ایموجی_ویژه روشن` / `.ایموجی_ویژه خاموش` | روشن/خاموش کردن ایموجی ویژه |
| `.بررسی_ایموجی روشن` / `.بررسی_ایموجی خاموش` | گزارش زندهٔ مراحل در Saved Messages |
| `.وضعیت_ایموجی` | گزارش کامل وضعیت + آمار |
| `.بستن` | بستن همه عملیات‌های در انتظار (قبلاً بود؛ برندینگ جدید) |
| `.عدم_حضور روشن` / `.عدم_حضور خاموش` | کنترل پیام عدم حضور |
| `.متن_عدم_حضور <متن جدید>` | تغییر متن پیام عدم حضور |

دستورهای انگلیسی قبلی (`.premium ...`، `.away ...`) همچنان کار می‌کنند.

## لاگ‌های فارسی

```
[بررسی ایموجی ویژه]
شناسه چت: -1001234567890
شناسه پیام: 123
Entity دارد: خیر (هیچ)
نوع پیام: عکس
نتیجه: ارسال دوباره کل آلبوم

[ارسال دوباره]
شناسه چت: Group (-777)
شناسه پیام: 123
پیام حذف شد: بله
پیام جدید ارسال شد: بله (msg=901)
```

---

## ۵) تست‌های اجباری (ماتریس مالک) — همگی پاس

هر سناریو آفلاین با شبیه‌سازی مرز شبکه اجرا شد؛ نتیجه الزامی هر مورد:
**پیام اول حذف شده + پیام دوم ایجاد شده + Custom Emoji فعال.**

| # | سناریو | تست | نتیجه |
|---|---|---|---|
| ۱ | متن (`🔥 تست`) | `test_1_text_message_delete_then_new_send` | ✅ |
| ۲ | Reply | `test_2_reply_delete_then_new_send` | ✅ (reply + فرمت حفظ) |
| ۳ | عکس با Caption | `test_3_photo_with_caption_delete_then_new_send` | ✅ |
| ۴ | ویدیو با Caption | `test_4_video_with_caption_delete_then_new_send` | ✅ |
| ۵ | آلبوم | `test_5_album_delete_then_new_send_as_one_album` | ✅ (یک‌جا) |
| ۶ | گروه | `test_6_group_delete_then_new_send` | ✅ |
| ۷ | خصوصی / Saved | `test_7_private_and_saved_delete_then_new_send` | ✅ |

تست‌های تکمیلی: ترتیب حذف قبل از ارسال (integration با Telethon واقعی)،
ممنوعیت مطلق EditMessageRequest (حتی در stub شبکه → AssertionError)، حذف
ناقص آلبوم → ارسال نمی‌شود، شکست حذف → بدون پیام تکراری، شکست ارسال →
retry + نجات محتوا، ضد حلقه، cooldown، خروجی shim سازگاری.

**جمع کل:** ۸۳۴ تست پاس — ۲۳ شکست از قبل و نامرتبط (transfer/flags/
translation/...؛ بدون هیچ تغییر نسبت به baseline قبل از این اصلاح).

---

## ۶) فایل‌های تغییرکرده

| فایل | تغییر |
|---|---|
| `services/emoji_resend_manager.py` | **جدید** — ماژول رسمی Delete + New Send |
| `services/premium_resend.py` | تبدیل به shim سازگاری |
| `services/premium_emoji_converter.py` | حذف کامل مسیر edit (injector + wrap) |
| `services/telegram_logger.py` | بلوک‌های `[بررسی ایموجی ویژه]` / `[ارسال دوباره]` |
| `services/away.py` | نام‌های فارسی پیام عدم حضور |
| `self.py` | دستورات فارسی + بازنویسی پاسخ‌ها + نصب مدیر جدید |
| `inline.py` | دکمه‌ها و پیام‌های پنل فارسی |
| `handlers/premium_emoji.py` | منوی ✨ ایموجی ویژه |
| `tests/test_premium_resend.py` | بازنویسی — ماتریس اجباری مالک |
| `tests/test_premium_check_debug.py` و ۵ فایل تست دیگر | هماهنگ با فرمت‌های جدید |
