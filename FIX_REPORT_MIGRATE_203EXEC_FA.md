# گزارش هات‌فیکس — خطای 203/EXEC در مهاجرت v0.09.18

**تاریخ:** 2026-09-18 | **نسخه migrate.sh:** 1.1 | **نسخه برنامه:** 0.09.18 (بدون تغییر)

## علامت (Symptom)

پس از دیپلوی v0.09.18 با دستور استاندارد (که `python3 -m venv .venv` می‌سازد)، سرویس
`selfbot` وارد حلقه‌ی خطای زیر شد و بالا نیامد:

```
selfbot.service: Main process exited, code=exited, status=203/EXEC
selfbot.service: Failed with result 'exit-code'.
Active: activating (auto-restart) (Result: exit-code)
```

## ریشه‌های اصلی (۳ نقص مستقل در migrate.sh v1.0)

### ریشه ۱ — هاردکد مسیر venv در نوشتن unit (علت مستقیم 203/EXEC)

تابع `prepare_venv` متغیر `VENV_PY` را درست حل می‌کند (اول `venv/`، بعد `.venv/`، بعد ساخت)
اما `setup_systemd` در **دو نقطه** مسیر را هاردکد کرده بود و به `VENV_PY` توجه نمی‌کرد:

- قالب ساخت unit جدید: `ExecStart=$TARGET_DIR/venv/bin/python main.py`
- بازنویسی unit موجود: `new_exec="$TARGET_DIR/venv/bin/python $args"`

وقتی دیپلوی فقط `.venv` دارد (دستور دیپلوی استاندارد)، unit به مسیری اشاره می‌کرد که وجود
نداشت → systemd نتوانست پایتون را exec کند → خطای 203 و حلقه‌ی ری‌استارت.

**رفع:** هر دو نقطه حالا از `$VENV_PY` واقعی استفاده می‌کنند؛ یعنی مسیری که واقعاً
اعتبارسنجی شده (وجود فایل + نصب requirements + تست import روی همان مفسر انجام می‌شود).

### ریشه ۲ — پوزیتیو کاذب در تأیید بالاآمدن سرویس

حلقه‌ی انتظار گام ۹ با **اولین** `is-active = active` خارج می‌شد. با `Type=simple` بین
لحظه‌ی start و شکست exec چند میلی‌ثانیه پنجره‌ی «active» گذرا وجود دارد؛ حلقه‌ی یک‌ثانیه‌ای
دقیقاً در همان پنجره توقف می‌کرد و پیام «✅ سرویس active است» چاپ می‌شد — در حالی که سرویس
در حلقه‌ی کرش بود (در لاگ واقعی سرور همین رخ داد).

**رفع:** بلوک «تأیید پایداری» اضافه شد: پس از دیدن active، اسکریپت ۳ ثانیه صبر می‌کند،
دوباره `is-active` و `MainPID` را می‌خواند؛ اگر سرویس غیر active باشد یا `MainPID=0/خالی`
باشد → rollback خودکار unit و توقف با پیام صریح «الگوی 203/EXEC یا کرش حلقه‌ای».

### ریشه ۳ — Description کهنه در unit

`Description` هرگز به‌روز نمی‌شد؛ روی سرور پس از دیپلوی v0.09.18 همچنان می‌نوشت
«PishroSelf v0.09.17 Tehran Time API».

**رفع:** `APP_VERSION` از `storage.py` نسخه هدف خوانده می‌شود و `Description` در هر دو مسیر
(ساخت unit جدید و بازنویسی unit موجود) به `PishroSelf v<نسخه>` به‌روز می‌شود؛ اگر unit خط
Description نداشته باشد، زیر `[Unit]` اضافه می‌شود.

## گاردهای جدید (پیشگیری از تکرار)

- گام ۵: اگر بعد از حل venv، پایتون قابل اجرا نبود → توقف با پیام صریح (قبل از هر تغییر unit).
- گام ۶: پیش از نوشتن unit دوباره `[ -x "$VENV_PY" ]` چک می‌شود — unit با مسیر اجرایی
  خراب **هرگز** نوشته نمی‌شود.
- DRY-RUN از این گاردها مستثناست (تا تست بدون سرویس واقعی ممکن بماند).

## تست‌ها (همه PASS)

هارنس سرتاسری با `python` / `systemctl` / `journalctl` قلابی و سناریوی دقیق سرور
(هدف فقط-.venv + unit خراب با مسیر venv بدون نقطه):

1. **مسیر سالم:** ExecStart به `.venv/bin/python` نوشته شد، Description به v0.09.18
   به‌روز شد، تأیید پایداری با MainPID واقعی پاس شد، مهاجرت تا انتها موفق.
2. **شبیه‌سازی 203/EXEC:** active گذرا + MainPID=0 → شناسایی شد، rollback اجرا شد،
   unit قدیمی برگشت، خروجی ۱ با پیام «الگوی 203/EXEC».
3. **DRY-RUN روی مخزن واقعی:** خروجی ۰، بدون هیچ تغییر جانبی.

## بازیابی سرور فعلی (اگر هنوز با هات‌فیکس دیپلوی نکرده‌اید)

```bash
systemctl stop selfbot
sed -i 's|^ExecStart=.*|ExecStart=<APP>/.venv/bin/python main.py|' /etc/systemd/system/selfbot.service
systemctl daemon-reload && systemctl start selfbot
```
(به‌جای `<APP>` مسیر واقعی نسخه جدید؛ مثلاً `/root/self/PishroSelf_v0.09.18_<ts>/PishroSelf`)

یا ساده‌تر: ZIP همین هات‌فیکس را استخراج و `bash migrate.sh <APP>` اجرا کنید — گام ۶ خودش
ExecStart را به `.venv` درست می‌کند و گام ۹ بالاآمدن واقعی را تأیید می‌کند.

## مهاجرت / دیتا

- هیچ — فقط `migrate.sh` عوض شده؛ دیتابیس، سشن‌ها، کانفیگ پایدار و APIها دست‌نخورده.
- کد برنامه دقیقاً همان v0.09.18 قبلی است (کامیت 2398dca)؛ این بسته فقط migrate.sh
  اصلاح‌شده (کامیت 5d41666) + این گزارش را اضافه دارد.
