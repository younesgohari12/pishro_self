# گزارش اصلاح معماری v0.09.16 — اتصال نسخه جدید به دیتای پایدار (راه ۱: Loader)

تاریخ: 2026-09-18

## مشکل

نسخه `PishroSelf_v0.09.15_COMPLETE` هنگام اجرا `API_ID` و `API_HASH` را از
`config.py` داخل سورس می‌خواند و به دیتای ثابت `/root/PishroSelfData` وصل
نمی‌شد. سه ایراد ریشه‌ای در لایه مسیرها وجود داشت:

1. `DATA_DIR = BASE_DIR/db` بود؛ یعنی دیتا داخل پوشه سورس ساخته می‌شد، نه در
   ریشه پایدار `/root/PishroSelfData`.
2. اعتبارنامه‌ها فقط از همان فایل سورس خوانده می‌شدند؛ فایل پایدار
   `/root/PishroSelfData/config.py` (منبع حقیقت سرور) هیچ‌جا خوانده نمی‌شد.
3. `DB_DIR = DATA_DIR` بود در حالی که چیدمان استاندارد storage، دیتابیس‌ها را
   در زیرپوشه `db/` ریشه دیتا انتظار دارد (`db/tabchi.sqlite3`،
   `db/index.json` و ...). به همین دلیل دو تست «فرایند تازه» در
   `test_storage_updates.py` سال‌ها در لیست شکست‌ها مانده بود.

## راه‌حل — config بارگذار (Loader)

`config.py` داخل سورس بازنویسی شد و الان فقط «بارگذار» است:

- **ریشه ثابت دیتا همیشه بیرون از سورس است:**
  - اجرای root (سرویس مالک): `DATA_DIR = /root/PishroSelfData` — همیشه.
  - سایر حساب‌ها: `<home>/PishroSelfData` (هم‌سو با `storage.default_data_dir()`).
- **DB_DIR همیشه `<DATA_DIR>/db` است** (چیدمان استاندارد storage) و
  SESSIONS_DIR/UPLOAD_DIR/MESSAGE_SAVE_CACHE_DIR/LOG_DIR/backup همگی زیر
  ریشه دیتا هستند؛ هیچ دیتایی داخل پوشه سورس ذخیره نمی‌شود.
- **تنظیمات حساس فقط از فایل پایدار خوانده می‌شود:**
  `/root/PishroSelfData/config.py` با پارس AST و «بدون اجرای هیچ کدی» (فقط
  انتساب‌های لیترال) خوانده و روی پیش‌فرض‌های امن سورس اعمال می‌شود. کلیدهای
  مسیر (`DATA_DIR` و ...) هرگز از فایل پایدار بازنویسی نمی‌شوند.
- **آپدیت‌های بعدی بدون تغییر دستی:** فایل پایدار «تنها منبع حقیقت» است و
  آپدیت نسخه‌ها هرگز آن را بازنویسی نمی‌کنند؛ `config.py` جدید (بدون هیچ
  رازی) کامیت شده و در ZIPهای بعدی هم هست، پس هر نسخه جدید از همان لحظه
  استخراج، به دیتای پایدار وصل است.
- **جداسازی تست:** در پروسه‌های pytest فایل پایدار هرگز خوانده نمی‌شود
  (قرارداد conftest: تست‌ها هرگز دیتای واقعی سرور را نمی‌بینند).
- قرارداد hardening v0.09.7 حفظ شد: بدون `os.getenv`/`os.environ`، بدون
  `.env`، `validate_runtime_config()` خالی، `LOG_LEVEL = 'ERROR'`.

## نتایج تست

- اجرای موازی قبل/بعد با همان کد: baseline قدیم 869 پاس / 23 شکست؛ با Loader
  **871 پاس / 21 شکست** — **صفر رگرسیون** و دقیقاً دو تست معماری
  (`test_two_fresh_processes...` و `test_billing_survives_two_processes...`)
  که انتظار همین طراحی را داشتند، FIXED شدند.
- اعتبارسنجی مستقل Loader در sandbox: خواندن امن فایل پایدار، پیروزی تنظیمات
  ذخیره‌شده بر پیش‌فرض نسخه جدید، کوک شدن نوع‌ها (int/str)، پین بودن مسیرها،
  سازگاری با تست runpy محیط/سخت‌گیر hardening.

## اسکریپت مهاجرت امن — `migrate.sh`

`migrate.sh` در ریشه ریپو، استقرار روی سرور را کامل و امن انجام می‌دهد
(۱۰ گام: توقف سرویس → بکاپ‌های timestamp دار شامل tar کامل دیتا → آماده‌سازی
کانفیگ پایدار با برداشت اعتبارنامه از نسخه‌های قبلی → نصب Loader → venv و
requirements → اصلاح `WorkingDirectory`/`ExecStart` در systemd →
daemon-reload → تست import → اجرا → لاگ ۱۰۰ خط آخر + راستی‌آزمایی مسیر دیتا،
سشن‌ها و فعال بودن ربات اصلی و اینلاین).

تضمین‌ها: هیچ فایل دیتایی کپی/حذف/overwrite نمی‌شود؛ مقادیر غیرخالی
اعتبارنامه هرگز تغییر نمی‌کنند (فقط جای خالی پر می‌شود)؛ در هر خطا rollback
خودکار به unit قبلی؛ `bash migrate.sh --rollback` برای برگشت دستی؛ برای
آپدیت‌های بعدی هم همان اسکریپت با مسیر نسخه جدید استفاده می‌شود.

تست اسکریپت: هارنس شبیه‌ساز سرور (systemd/journalctl شبیه‌سازی‌شده) با ۴
سناریو و ۳۲ assertion — همه پاس: مسیر خوشحال، برداشت اعتبارنامه از نسخه
قبلی، DRY-RUN بدون هیچ تغییری، rollback موفق؛ و در همه سناریوها دیتای
`PishroSelfData` بایت‌به‌بایت دست‌نخورده ماند.
