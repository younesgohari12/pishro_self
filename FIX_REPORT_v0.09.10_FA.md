# گزارش اصلاح v0.09.10

## علت

مسیر و فرمت سشن در دو ZIP یکسان بود. `start_session` قبل از خواندن فایل، `can_run` را بررسی می‌کرد؛ برای حساب بدون رکورد، موجودی صفر تشخیص داده می‌شد و هیچ اتصال یا بازیابی اطلاعات انجام نمی‌شد. خود فایل سشن در این مسیر حذف نمی‌شد.

## اصلاح

- مسیر بازیابی هنگام startup از شرط داشتن رکورد و اعتبار جدا شد. مسیر فعال‌سازی معمولی و مجوز مصرف سرویس همچنان کنترل می‌شود.
- حساب فقط بعد از `is_user_authorized` و `get_me` معتبر ثبت می‌شود؛ نام فایل به‌تنهایی مبنای هویت نیست.
- بازیابی اطلاعات تکرارپذیر است؛ سشن و موجودی موجود بازنویسی نمی‌شوند و Trial تمدید نمی‌شود.
- اتصال در خطای راه‌اندازی و لغو task درست بسته می‌شود؛ شکست یک فایل به سایر سشن‌ها سرایت نمی‌کند.
- برای اتصال‌های بازیابی‌شده و بیکار، صورتحساب اجرا نمی‌شود. کنترل اعتبار برای هندلرهای جانبی نیز اعمال شد تا صرف اتصال به معنی فعال شدن امکانات نباشد.
- کنترل سراسری دو قابلیت غیرفعال‌شده در دکمه‌ها، دستورها و stateهای باقی‌مانده اعمال شد. AI فعال است.
- کانفیگ Python مستقل با تنظیمات ارسالی جایگزین بارگذاری env شد.
- Telethon 1.45.0 ساختار دکمه متفاوتی دارد و یکی از تست‌های نسخه ورودی را می‌شکست. نسخه 1.44.0 ثابت و با کل مجموعه تست‌ها بررسی شد.

## اعتبار شواهد

۳۴۰ تست موفق روی نسخه نهایی در Python 3.12. تست‌های کیف پول، پرداخت، انتقال، ورود، Trial، مدیریت، ایموجی، AI و قابلیت‌های قبلی نیز اجرا شدند. دو تست قدیمی مخصوص الزام env طبق درخواست شما با تست کانفیگ مستقیم و عدم اثرگذاری environment جایگزین شدند.

تست اختصاصی شامل سشن بدون هیچ رکورد، مهاجرت JSON، حفظ اولویت SQLite، حساب خاموش، Trial منقضی، اجرای مجدد، BOM/CRLF، چند سشن همزمان، فایل خراب، تطبیق هویت، خطای شبکه، خطای ذخیره، لغو اجرا، صورتحساب واقعی با شبکه ساختگی، و مسدود بودن API از مسیرهای سلف و پنل است.

تست زنده Telegram/AvalAI با توکن‌ها و سشن‌های شما انجام نشده است.
