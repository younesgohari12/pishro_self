# گزارش نسخه 0.09.11

هدف: جدا کردن چرخه انتشار کد از داده‌های جاری و حفظ اطلاعات کاربران در آپدیت‌های بعدی.

مسیرهای قبلی کنار کد بودند و استخراج نسخه در پوشه جدید می‌توانست دیتابیس دیگری انتخاب کند. تنظیمات صدا و فهرست متنی هم در ریشه پروژه قرار داشتند. اکنون داده و کانفیگ شخصی در یک ریشه ثابت خارج از برنامه ذخیره می‌شوند و قالب فایل‌های اولیه فقط در `defaults` قرار دارد.

`storage.py` مسئول قفل اجرای مشترک، واردکردن اولیه، تشخیص نسخه داده، بررسی سلامت، بکاپ با WAL، نگاشت مسیر مدیا، ثبت ارتقای موفق و restore با حفظ داده فعلی است. تغییر ساختار در آینده باید نسخه مهاجرت مشخص داشته باشد؛ فرمت بالاتر خودکار پایین آورده نمی‌شود.

`main.py` ابتدا محل داده را آماده و بکاپ پیش از تغییر را تأیید می‌کند، سپس کانفیگ شخصی را بارگذاری و ماژول‌های Runtime را import می‌کند. بنابراین در نخستین اجرای پس از انتقال هم توکن و تعرفه قبلی اعمال می‌شود. کیف پول SQLite مرجع باقی مانده و موجودی JSON قدیمی دوباره اعتباردهی نمی‌شود.

خواندن JSON خراب بدون بکاپ سالم دیگر به یک دیکشنری خالی تبدیل نمی‌شود. وجود رکورد نشانی انتقال در نسخه قدیمی از واردکردن مجدد داده کهنه جلوگیری می‌کند. هیچ داده جاری در بسته انتشار قرار ندارد.

بکاپ خودکار در کنار برنامه اجرا می‌شود و برای snapshot از قفل‌های نوشتن JSON/SQLite/تنظیمات صدا استفاده می‌کند. شکست بکاپ قبل از ارتقا اجازه اجرای Runtime را نمی‌دهد؛ شکست بکاپ دوره‌ای ثبت می‌شود و داده موجود را تغییر نمی‌دهد.

نتیجه: ۳۶۳ تست موفق شامل ۳۴۰ تست پایه و ۲۳ سناریوی جدید. مهاجرت تمام جدول‌های fixture، حفظ تراکنش‌ها و موجودی، حفظ تنظیمات شخصی، سشن و مدیا، تغییر مسیر مدیا، اجرای دوباره، دو پوشه و فرایند تازه، WAL، خرابی/فقدان داده، پوشه اشتباه، قفل اجرا، خرابی ZIP و شکست انتشار restore بررسی شدند. Syntax تمام ۱۰۱ فایل Python بررسی شد.

محدودیت واقعی: تست زنده حساب‌های شما انجام نشده و Windows در این محیط اجرا نشده است. پوشه داده و بکاپ محلی در برابر ازبین‌رفتن کل دیسک تضمین ایجاد نمی‌کنند؛ برای آن باید بکاپ جدا از سرور نگهداری شود. حفظ رفتار نسخه‌های بعدی به رعایت قرارداد ثبت‌شده در AGENTS.md وابسته است.
