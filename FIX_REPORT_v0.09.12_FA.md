# گزارش تغییرات نسخه 0.09.12

- حسابگر از زمان اجرای تسک به زمان پایدار حساب در SQLite منتقل شد: ۱ الماس در ۱۸۰۰ ثانیه، تسویه خاموشی در شروع مجدد و بررسی مستقل هر ثانیه.
- جدول `billing_clocks` به‌صورت افزایشی ایجاد می‌شود. مقدار قدیمی `wallet_usage` یا در نبود آن `bill_seconds` یک بار منتقل می‌شود. هیچ موجودی یا سابقه‌ای از JSON دوباره شارژ نمی‌شود.
- زمان، موجودی و دفتر تراکنش با هم commit/rollback می‌شوند. تسویه پیش از تغییر کیف پول و ثبت دقیق انتقال صفر/شارژ در مسیر پرداخت، بازی، انتقال و مدیریت فعال است. عملیات پنل مدیر و هزینه شروع از تغییر اتمیک استفاده می‌کنند تا مقدار کهنه روی موجودی جدید نوشته نشود.
- دوره رایگان، توقف دستی و تمام‌شدن اعتبار حفظ می‌شوند. قطع شبکه و خاموشی سرور، حسابگر سرویس فعال را متوقف نمی‌کند.
- اعلان‌ها از حسابگر جدا شدند. خطای شبکه ارسال هشدار، جلوی ثبت هزینه را نمی‌گیرد. پایان موجودی با همان کنترل دسترسی موجود، امکانات سلف را غیرفعال می‌کند و اعتبار یا سشن تازه اختراع نمی‌شود.
- ساختار داده از فرمت ۱ به ۲ ارتقا می‌یابد و فرمت قبلی برای بازیابی قابل خواندن است. بکاپ قبل از ارتقا، مسیر ثابت داده و کانفیگ شخصی حفظ شده‌اند. حذف جدول ضروری زمان‌بندی باعث توقف شروع برنامه می‌شود.
- ترجمه ربات اصلی حذف شد؛ دستور `.ترجمه` در سلف متن مستقیم، متن/کپشن ریپلای، زبان مقصد دلخواه و مبدا/مقصد صریح را می‌پذیرد. املای «انگیلیسی» هم پذیرفته می‌شود.
- فلگ جدید `SELF_TRANSLATE_ENABLED` روی نسخه‌های قبلی با فلگ قدیمی خاموش کار می‌کند. پیش‌فرض مترجم AvalAI با مدل قابل تنظیم `gpt-4o` و کلید موجود است. API رسمی Google Cloud نیز با کلید جداگانه قابل انتخاب است.
- متن بلند بخش‌بندی می‌شود؛ پاسخ قطع‌شده دوباره با بخش کوتاه‌تر درخواست می‌شود و خروجی ناقص کش نمی‌شود. خروجی بدون parse_mode ارسال می‌شود. خطای خام API در چت نمایش داده نمی‌شود.
- قیمت ارز همچنان خاموش است. env برنگشته؛ کانفیگ Python و اطلاعات قبلی حفظ شده‌اند.

۴۰۲ تست موفق؛ نتیجه کامل در TEST_RESULTS_v0.09.12.txt و روش ارتقا/استفاده در README_FIRST_FA.md. درخواست واقعی ترجمه و ورود واقعی به تلگرام انجام نشده است.
