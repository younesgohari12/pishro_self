# گزارش اصلاح پیام عدم حضور + مدیریت وضعیت آنلاین/آفلاین — v0.09.14

**موضوع:** اصلاح کامل سیستم پیام عدم حضور (Away) و ساخت Presence Manager
**نسخه مبنا:** v0.09.13 (ریلیز Premium Resend) → **v0.09.14**

---

## ۱) مشکلاتی که رفع شد

### مشکل اول — اسپم پیام عدم حضور
رفتار قبلی (معیوب): هر پیام خروجی مالک = «برگشت آنلاین» تلقی می‌شد و کل
`away_sent_users` را ریست می‌کرد؛ نتیجه: بعد از هر پیامِ مالک، دوباره به همه
پیام عدم حضور می‌رفت (اسپم) و مسیر بازخورد به خودش امکان loop می‌داد.

رفتار جدید (مطابق spec):
- محدودیت **per-chat** است، نه global: بعد از هر آفلاین شدن، در هر چت فقط
  **یک بار** پیام عدم حضور ارسال می‌شود.
- مثال معیار پذیرش:
  - Chat A: «سلام» → یک پیام عدم حضور ✅
  - Chat A: «خوبی؟» → هیچ پیامی ارسال نمی‌شود ✅
  - Chat B: «سلام» → یک پیام عدم حضور (مدیریت جدا از Chat A) ✅

### مشکل دوم — آنلاین شدن خودکار اکانت
قبلاً هیچ کنترلی روی وضعیت آنلاین وجود نداشت؛ سرور تلگرام هر
`sendMessage` / `setTyping` / `readHistory` را به معنای Online شدن اکانت
پردازش می‌کرد. حالا:

- ❌ `send_message` / `send_file` → باعث Online می‌شود ولی Presence Manager
  بلافاصله بعد از موج ارسال، اکانت را دوباره **Offline** می‌کند.
- ❌ `SetTypingRequest` (typing action) → در حالت Away **هیچ typing action
  ارسال نمی‌شود** (درخواست کلاً مسدود می‌شود).
- ❌ `ReadHistoryRequest` و کل خانواده Read (read acknowledgement غیرضروری)
  → در حالت Away مسدود می‌شوند.
- ❌ `UpdateStatusRequest(offline=False)` (آنلاین کردن دستی) → مسدود می‌شود.
- ✅ `get_messages` و درخواست‌های فقط‌خواندنی → آزاد عبور می‌کنند؛ وضعیت را
  تغییر نمی‌دهند.
- هدف محقق شد: وقتی Away فعال است، اکانت **Offline می‌ماند**.

---

## ۲) معماری جدید

### ماژول جدید: `services/presence_manager.py`
مسئولیت‌ها:
- مدیریت وضعیت واقعی آنلاین/آفلاین هر حساب سلف.
- نصب یک wrapper روی `client._call` (نقطه ورود «همه» درخواست‌های Telethon) —
  یعنی پوشش کامل `send_message`, `send_file`, `send_read_acknowledge`,
  `send_chat_action` و هر چیز دیگری که از کلاینت عبور می‌کند.
- Classification درخواست‌ها: `typing` / `read` / `send` / `status` / `other`.
- در حالت Away:
  - مسدودسازی کامل typing + read + آنلاین‌سازی دستی.
  - بعد از هر موج ارسال (با debounce ۱.۲ ثانیه‌ای) یک‌بار
    `UpdateStatusRequest(offline=True)` از مسیر اصلی ارسال می‌شود تا اکانت در
    حداقل زمان دوباره Offline نمایش داده شود.
- در حالت عادی (Away خاموش): wrapper **کاملاً شفاف** است؛ هیچ درخواستی مسدود
  یا اضافه نمی‌شود و رفتار عادی تلگرام حفظ می‌شود.
- ضد-loop: خودِ `UpdateStatusRequest(offline=True)` مجاز است و هرگز دوباره
  زمان‌بندی نمی‌کند؛ هیچ مسیری از پیام خروجی به ریست Away وصل نیست.
- خواندن پرچم Away از دیتابیس با کش ۲ ثانیه‌ای (الگوی converter/resend
  پروژه) تا هیچ ارسالی منتظر دیتابیس نماند.

### بازنویسی: `services/away.py`
- ساختار دیتابیس جدید (دقیقاً مطابق spec مالک):
  - `away_enabled` — روشن/خاموش
  - `away_text` — متن پیام
  - `away_active_session` — شناسه سشن واقعی جاری
  - `away_sent_chats` — `{chat_id: timestamp}` (per-chat)
  - `away_sent_users` (قدیمی) فقط برای سازگاری داده‌های موجود نگه داشته شد.
- منطق دریافت پیام خصوصی:
  ```
  if away_enabled:
      if chat_id not in away_sent_chats:
          send away message
          away_sent_chats[chat_id] = now
      else:
          ignore
  else:
      ignore
  ```
- **Reset Logic** — لیست فقط و فقط این‌جاها پاک می‌شود:
  - دستور `.عدم_حضور خاموش` (و خاموش کردن از پنل)
  - روشن کردن دوباره (چرخه خاموش/روشن = دوره تازه)
  - «پاک کردن لیست» دستی (`.عدم_حضور ریست` / دکمه پنل)
  - شروع یک **Session جدید واقعی** (restart سلف)
- تشخیص آنلاین شدن از هیچ‌کدام از این‌ها انجام **نمی‌شود**:
  ❌ هر پیام خروجی | ❌ send_message | ❌ event outgoing | ❌ typing
  (کل هندلر outgoing قدیمی حذف شد — امکان loop و اسپم به‌طور ساختاری رفع شد.)
- چت‌های ساکت (سکوت) و دشمن هرگز پاسخ خودکار نمی‌گیرند؛ بات‌ها و پیام‌های
  سرویس هم نادیده گرفته می‌شوند.

---

## ۳) پنل فارسی — «پیام عدم حضور»

مسیر: `.پنل` → دکمه «💤 پیام عدم حضور»

| گزینه | عملکرد |
|---|---|
| 🟢 روشن کردن / 🔴 خاموش کردن | فعال/غیرفعال + ریست لیست |
| ✏️ تغییر متن | دریافت متن جدید در همان چت (یا لغو با `.بستن`) |
| 🧹 پاک کردن لیست | پاک‌سازی «لیست چت‌های پاسخ داده شده» |
| ↩️ بازگشت | برگشت به پنل اصلی |

- صفحه پنل تعداد و نمونه شناسه‌های «چت‌های پاسخ داده شده» را نشان می‌دهد.
- توضیح رسمی پنل: اکانت آفلاین می‌ماند (بدون typing / read / status) و
  پیام‌های خروجی هرگز لیست را ریست نمی‌کنند.

## ۴) دستورهای فارسی

| دستور | عملکرد |
|---|---|
| `.عدم_حضور روشن` | روشن کردن + شروع دوره تازه (لیست پاک می‌شود) |
| `.عدم_حضور خاموش` | خاموش کردن + پاک شدن لیست |
| `.عدم_حضور` / `.away` | نمایش وضعیت، متن و تعداد چت‌های پاسخ داده شده |
| `.عدم_حضور ریست` / `.away reset` | پاک کردن لیست چت‌ها |
| `.متن_عدم_حضور <متن>` | تغییر متن پیام |

## ۵) لاگ‌های دیباگ فارسی

```
[پیام عدم حضور]
شناسه چت: 123456789
وضعیت: روشن
نتیجه: ارسال شد          ← یا «قبلاً ارسال شده» / «ارسال ناموفق (...)»
```

```
[مدیریت وضعیت]
Online Trigger: send_message     ← یا typing / read / status_online
منبع: send_message               ← یا typing / read / manual
اقدام: اجازه شد؛ بازگردانی آفلاین زمان‌بندی شد   ← یا «مسدود شد — ...»
```

- «قبلاً ارسال شده» و رویدادهای پرتکرار فقط در فایل لاگ محلی می‌روند
  (`telegram=False`) و تلگرام را شلوغ نمی‌کنند؛ ضد-اسپم مستقل ۳ ثانیه‌ای
  برای `[مدیریت وضعیت]` اضافه شد (`PRESENCE_DEBUG` / `PRESENCE_LOG_LEVEL`).

---

## ۶) تست‌های اجباری (۴ سناریوی مالک)

| # | سناریو | نتیجه |
|---|---|---|
| ۱ | Chat A «سلام» → یک پیام؛ Chat A «خوبی؟» → هیچ؛ Chat B «سلام» → یک پیام | ✅ `test_matrix_chat_a_once_then_nothing_chat_b_once` |
| ۲ | خاموش/روشن کردن Away → لیست ریست و چت‌ها دوباره یک‌بار پیام می‌گیرند | ✅ `test_toggle_off_then_on_resets_list` |
| ۳ | ارسال پیام عدم حضور اکانت را Online نمی‌کند: پیام عبور می‌کند، `offline=True` خودکار برمی‌گردد، هیچ typing/آنلاین‌سازی وجود ندارد | ✅ `test_away_reply_send_does_not_stay_online` |
| ۴ | چند پیام پشت سر هم → فقط یک پاسخ | ✅ `test_multiple_messages_in_a_row_get_one_response` |

تست‌های تکمیلی: مسدودسازی typing/read/status در حالت Away، شفاف بودن کامل
وقتی Away خاموش است، debounce موج ارسال‌ها (۴ ارسال → فقط ۱ بازگردانی
آفلاین)، ریست با Session جدید، حذف هندلر outgoing (ضد-loop)، نصب/برداشتن
پاک، فرمت لاگ‌های فارسی.

**نتیجه اجرا:** ۴۷ تست جدید/به‌روز Away+Presence+تنظیمات: همگی پاس.
کل مجموعه: **847 پاس / 23 شکست** — دقیقاً برابر baseline قبل از تغییر
(همه نامرتبط: transfer/flags/translation/storage) → **صفر رگرسیون**.

**محدودیت‌ها:** تست‌ها آفلاین و با کلاینت تقلبی اجرا شده‌اند؛ ادعای لاگین
واقعی تلگرام وجود ندارد. نمایش Online/Offline نهایی همیشه در اختیار سرور
تلگرام است؛ Presence Manager بازه آنلاین نمایش‌داده‌شده را به کمترین حد
ممکن (لحظه ارسال + debounce) می‌رساند.

## ۷) سازگاری ارتقا (قرارداد داده مالک)

- هیچ کلید دیتابیس حذف یا ریست نمی‌شود؛ `away_sent_users` قدیمی نرمال‌سازی
  و نگه‌داری می‌شود (فقط دیگر استفاده نمی‌شود).
- شروع برنامه (سشن جدید) طبق spec، لیست `away_sent_chats` را پاک می‌کند —
  یعنی ارتقا رفتار in-flight را هم پاک و قابل پیش‌بینی می‌کند.
- `AWAY_RESET_HOURS` دیگر استفاده نمی‌شود (ریست TTL با spec جدید ناسازگار
  بود)؛ کلید در config کاربران دست‌نخورده باقی می‌ماند.
