# نسخه 0.09.1 — اصلاح انتقال الماس و اتصال حساب

مبنای این نسخه همان فایل ارسالی شماست. پنج فایل قبلی تغییر کرده‌اند:
`bot/core.py`، `database/models.py`، `db.py`، `login_manager.py` و `ui/__init__.py`.
دو سرویس جدید برای انتقال و پاداش دعوت و تست‌های مربوط اضافه شده‌اند.

## اصلاحات

- مقصد انتقال باید قبلاً با ربات تعامل و ثبت‌نام کرده باشد. داشتن یک رکورد کیف پول به‌تنهایی کافی نیست؛ بنابراین رکوردهای جعلی ساخته‌شده توسط نسخه قبل پذیرفته نمی‌شوند.
- حساب مقصد در مرحله انتخاب و هنگام تأیید نهایی از تلگرام بررسی می‌شود. حساب حذف‌شده، ربات، خود فرستنده و مقصد غیرقابل‌بررسی رد می‌شوند. اگر بررسی حساب ممکن نباشد، هیچ الماسی کسر نمی‌شود؛ مقصد باید دوباره ربات را استارت کند.
- مبلغ باید عدد صحیح مثبت، در محدوده مجاز و حداقل ۱۰ الماس باشد. حداقل و درصد کارمزد همچنان از تنظیمات قبلی خوانده می‌شوند. برای ۱۰ الماس، ۱ الماس کارمزد و ۹ الماس دریافتی ثبت می‌شود. گردکردن کارمزد مانند قبل half-even است، با محاسبه Decimal به‌جای float.
- کسر موجودی، واریز، ثبت کارمزد در رسید و دو رکورد دفتر تراکنش، همگی در یک تراکنش SQLite انجام می‌شوند. خطای میانه کار باعث rollback می‌شود.
- هر تأیید شناسه یکتا دارد. دکمه قدیمی انتقال تازه را تأیید نمی‌کند و درخواست تکراری دوباره از موجودی کم نمی‌کند. متن «لغو» در مرحله تأیید نیز کار می‌کند.
- شماره تایپ‌شده و مخاطب دیگر برای اتصال رد می‌شوند. کاربر باید در گفتگوی خصوصی دکمه «ارسال شماره من» را بزند. **اتصال حساب دیگری از این مسیر مجاز نیست.**
- پاسخ واقعی ارسال کد، نوع پاسخ، hash و طول کد بررسی می‌شوند. پاسخ نامعتبر یا روش پشتیبانی‌نشده صفحه کد تولید نمی‌کند. خطای شماره ثبت‌نشده مدیریت می‌شود؛ ثبت‌نام حساب جدید انجام نمی‌شود.
- ارسال هم‌زمان چند درخواست ورود مهار شده است. فاصله درخواست‌های کد ۶۰ ثانیه است؛ FloodWait تلگرام رعایت می‌شود. این محدودیت محلی در حافظه فرایند است. نشست ورود ۱۰ دقیقه اعتبار دارد.
- دکمه کد به کاربر، چت خصوصی، پیام صفحه‌کلید و مرحله ورود متصل است. پیش از ذخیره سشن، شناسه حساب واردشده باید با کاربر ربات برابر باشد؛ این شرط هم برای نصب و هم تست رایگان اعمال می‌شود. فایل سشن مجوز 600 می‌گیرد.
- دعوت‌کننده باید ثبت‌نام کرده باشد. کاربری که قبلاً ثبت‌نام کرده نمی‌تواند بعداً خود را زیرمجموعه جدید معرفی کند. برای هر دعوت‌شونده، فقط یک پاداش در SQLite ثبت می‌شود؛ ثبت claim و افزایش موجودی اتمی است. پرچم پاداش قدیمی نیز رعایت می‌شود.
- شکست ثبت موجودی در SQLite دیگر به‌عنوان موفقیت صرفاً در JSON ذخیره نمی‌شود. موجودی منفی، اعشاری، bool و خارج از محدوده رد می‌شود.

## نصب روی نسخه فعلی

1. ربات و فرایندهای وابسته را متوقف کنید و از پوشه پروژه، مخصوصاً `db` و `sessions`، پشتیبان بگیرید.
2. محتوای پوشه `0.09` این ZIP را روی پوشه پروژه موجود کپی کنید. `config.py` داخل بسته همان فایل ارسالی شماست؛ اگر تنظیمات سرور را پس از آن تغییر داده‌اید، تنظیمات فعلی سرور را حفظ کنید.
3. پوشه‌های `db`، `sessions`، `message_cache` و رسانه‌های زمان اجرا عمداً داخل بسته به‌روزرسانی قرار نگرفته‌اند. آنها را از سرور حذف یا با پوشه خالی جایگزین نکنید.
4. ربات را با همان روش قبلی اجرا کنید. جدول‌های جدید در همان دیتابیس به‌صورت خودکار و بدون حذف جدول‌های قبلی ایجاد می‌شوند.
5. برای اتصال حساب، از دکمه «ارسال شماره من» استفاده کنید. برای دریافت الماس، مقصد ابتدا ربات را استارت کند.

## نتیجه بررسی

**۹۷ تست پاس؛ صفر شکست.** این عدد شامل تست‌های موجود پروژه و تست‌های جدید است.

تست‌های جدید شامل دو شناسه گزارش‌شده، کیف پول جعلی قدیمی، مبلغ منفی و غیرمعتبر، موجودی ناکافی، سرریز، ۲۰ تأیید هم‌زمان، ۲۰ انتقال رقابتی، خطای عمدی پس از کسر و rollback، پاداش دعوت هم‌زمان، ورود با شماره دستی/مخاطب دیگر، لغو درخواست در حال اجرا، ورود موفق همان حساب، رمز دو مرحله‌ای و اتصال واقعی handlerهای پنل به سرویس‌های اصلاح‌شده هستند.

آزمون‌ها با SQLite موقت و پاسخ‌های شبیه‌سازی‌شده تلگرام اجرا شده‌اند. **تست اتصال زنده، ارسال کد واقعی و انتقال روی حساب شما انجام نشده است.** محدودیت‌های احتمالی شبکه، مجوزهای تلگرام و تنظیمات سرور باید هنگام نصب بررسی شوند. این گزارش ادعای ممیزی کامل امنیتی تمام قابلیت‌های پروژه نیست.

موجودی‌های تاریخی و انتقال‌های قبلی خودکار برگشت داده نشده‌اند. داده‌ها و سشن‌های فایل ورودی تغییر نکرده‌اند. مرجع انتقال‌های جدید `wallet_transfers` و مرجع پاداش‌های جدید `referral_claims` است. شمارنده‌های نمایشی قدیمی JSON پس از ثبت مالی به‌روزرسانی می‌شوند؛ اگر نوشتن JSON شکست بخورد، ممکن است شمارنده نمایشی عقب بماند، اما انتقال یا پاداش دوباره انجام نمی‌شود. برای حفظ سازگاری با CHECK قدیمی، رکوردهای دفتر `diamond_transactions` با نوع ADMIN_ADD/ADMIN_REMOVE و شرح WALLET_TRANSFER یا REFERRAL نوشته می‌شوند؛ نوع واقعی عملیات در جدول اختصاصی مشخص است.

## نکته درباره گزارش شماره ناموجود

نمایش صفحه کد، به‌تنهایی وجود حساب یا تحویل کد را اثبات نمی‌کند. API تلگرام پاسخ ارسال کد و مرحله احراز هویت را جدا می‌کند؛ مسیر ورود ممکن است نیاز به ثبت‌نام را در مرحله احراز هویت مشخص کند. این نسخه از API فرضی تشخیص قطعی شماره پیش از ورود استفاده نمی‌کند؛ شماره ورودی را به مخاطب خود کاربر محدود می‌کند و هویت نهایی را کنترل می‌کند.

مراجع رسمی:
- [Telegram auth.sendCode](https://core.telegram.org/method/auth.sendCode)
- [Telegram auth.sentCode](https://core.telegram.org/constructor/auth.sentCode)
- [Telegram auth.signIn](https://core.telegram.org/method/auth.signIn)

برای اجرای تست‌ها در محیط توسعه: `python -m pip install -r requirements.txt pytest` سپس `python -m pytest -q tests`.
