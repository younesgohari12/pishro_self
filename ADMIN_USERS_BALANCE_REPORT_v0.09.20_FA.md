# گزارش ریشه‌ای v0.09.20 — اپشن افزایش/کاهش موجودی در پنل مدیریت → بخش کاربران

**تاریخ:** ۱۴۰۴/۰۶/۲۷ — **نسخه:** 0.09.20 — **کامیت:** روی main + تگ `v0.09.20-admin-users-balance-fa`

---

## ۱) علامت

مالک گزارش داد: «اپشن کاهش و افزایش موجودی داخل بخش پنل مدیریت، بخش کاربران بگذار و مشکل را از ریشه درست کن» — یعنی در پنل مدیریت → 👤 مدیریت کاربران، امکان افزایش/کاهش موجودی الماس کاربران عملاً وجود نداشت.

## ۲) ریشه‌یابی (سه ریشه قطعی)

1. **UI یتیم — ریشه اصلی:** تابع `render_user_detail` (با دکمه‌های ➕ الماس / ➖ الماس) در `handlers/admin.py` وجود داشت اما **از هیچ منویی قابل دسترس نبود**؛ فقط بعد از تکمیل یک تغییر موجودی رندر می‌شد که خودِ آن هرگز قابل شروع نبود. نمای واقعیِ بخش کاربران `render_user_management_detail` بود که **فقط دکمه بن/آن‌بن دارد** — پس اپشن افزایش/کاهش موجودی برای مدیر هرگز دیده نمی‌شد.
2. **ناسازگاری منبع داده:** لیست کاربران از JSON + `user_profiles` ساخته می‌شود ولی نمای جزئیات به رکورد جدول `users` در SQLite وابسته بود؛ برای کاربری که هنوز رکورد کیف پول نداشت، کلیک روی نامش → «❌ کاربر پیدا نشد». جستجوی کاربر (`USER_SEARCH`) هم فقط جدول کیف پول SQLite را می‌گشت و کاربرِ بدون رکورد پیدا نمی‌شد.
3. **تست کهنه:** `test_admin_user_pagination.py` با API قدیمی صفحه‌بندی (`db.get_all_user_ids`) نوشته شده بود و از شکست‌های تاریخی (۲۱تایی) بود.

## ۳) رفع ریشه‌ای

### نمای واحد کاربر (`handlers/admin.py`)
- `render_user_detail` بازنویسی شد به **نمای واحد و کامل کاربر**: ID، یوزرنیم، نام، تاریخ عضویت، آخرین فعالیت، وضعیت Trial + انقضا، تعداد تیکت، رفرال، **💎 موجودی**، وضعیت حساب (فعال/بن‌شده).
- **همیشه رندر می‌شود** — حتی بدون رکورد کیف پول (موجودی از لایه `db.get_user_settings` خوانده می‌شود که SQLite را روی JSON پوشش می‌دهد).
- دکمه‌های نمای واحد: `➕ افزایش موجودی` | `➖ کاهش موجودی` → بن/آن‌بن (بر اساس وضعیت) → 🎁 مدیریت تست → ↩️ کاربران.
- `render_user_management_detail` اکنون همان نمای واحد را برمی‌گرداند (سازگاری عقب برای همه فراخوانی‌ها: لیست کاربران، جستجو، بعد از بن/آن‌بن).

### جریان تغییر موجودی (یک مسیر مشترک)
- `adm2_uplus_ / adm2_uminus_` → پرامپت با **نمایش موجودی فعلی** + دکمه‌های سریع (+۱۰/+۵۰/+۱۰۰ و +۵۰۰/+۱۰۰۰) + ورود دستی مقدار.
- `adm2_uquick_<delta>_<amount>_<target>` — تغییر یک‌ضربه‌ای از دکمه‌های سریع.
- ورود دستی (`USER_BALANCE_AMOUNT`): پذیرش کاما، اعتبارسنجی عدد مثبت، گارد نشست منقضی.
- `_adjust_user_balance` مسیر واحد هر دو روش: تغییر فقط از لایه رسمی `balance_service.admin_adjust` (دفتر کل append-only + ledger)، خاموشی موقت هشدار موجودی، اطلاع به مدیر (قبل/بعد) و **اطلاع به کاربر**، بازگشت به نمای تازه‌شده کاربر.

### جستجوی کاربر
- اگر جدول کیف پول جواب نداد، از `resolve_user` (ID عددی / یوزرنیم ثبت‌شده / تلگرام) ادامه می‌دهد — دیگر «کاربر پیدا نشد» کاذب وجود ندارد.

### پنل مدیریت کیف پول (`bot/core.py`)
- دکمه جدید **«👥 بخش کاربران»** به منوی 🛡 پنل مدیریت اضافه شد → همان بخش کاربران کامل (لیست/جستجو/جزئیات/موجودی) را باز می‌کند؛ هر دو پنل اکنون یک بخش کاربران مشترک دارند.

## ۴) تست‌ها

- **۱۴ تست جدید** در `tests/test_admin_users_balance_ui.py`:
  نمای واحد بدون رکورد کیف پول (دکمه‌های ➕/➖ حتماً موجود)، مسیر کال‌بک `adm2_user_` → نمای واحد، delegating سازگاری عقب، پرامپت با پیش‌فرض‌ها و موجودی فعلی، دلتای منفی کاهش، دکمه سریع افزایش (مقدار + اطلاع کاربر + قبل/بعد)، دکمه سریع کاهش، رد payload نامعتبر، ورود دستی موفق + پاک‌سازی state، رد ورودی غیرعددی، fallback جستجو برای کاربر بدون رکورد، اولویت جدول کیف پول در جستجو، رد بدون مجوز `manage_users`، وجود دکمه «بخش کاربران» در منوی مدیریت.
- **تست کهنه صفحه‌بندی** با API فعلی اصلاح شد (شکست تاریخی → پاس).
- **سوت کامل:** ۹۵۲ پاس / ۲۰ شکست — مقایسه دقیق FAILED با baseline v0.09.19 (۹۳۷/۲۱): `comm` هر دو جهت → تنها تفاوت = همان تست صفحه‌بندی که **اصلاح شد**. **صفر رگرسیون.**

## ۵) دیپلوی

```bash
cd /root/self
# استخراج ZIP نسخه جدید، سپس:
bash migrate.sh /root/self/<پوشه-جدید>/PishroSelf
```

- **Migration دیتایی: هیچ** — دیتابیس/سشن/کانفیگ دست‌نخورده. فقط کد.
- تغییرات فقط در `handlers/admin.py`، `bot/core.py` (یک دکمه)، `storage.py` (ورژن) و تست‌هاست.
- بعد از دیپلوی: تلگرام → پنل اصلی → «👑 مدیریت» → «👤 مدیریت کاربران» → انتخاب کاربر → «➕ افزایش موجودی» یا «➖ کاهش موجودی» → مقدار را بفرست یا دکمه سریع بزن. کاربر بلافاصله پیام تغییر موجودی دریافت می‌کند.
